import 'package:flutter/material.dart';

class helppage extends StatelessWidget {
  const helppage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("How to get a list of companies? In ITS system"),
    );
  }
}
